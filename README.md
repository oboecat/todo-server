### To-do item
{
    body: string,
    completed: boolean,
    id: string
}

## Endpoints

* get to-do list
GET /

* post new item
POST /

* patch changes to existing item
PATCH /:id

* delete item
DELETE /:id